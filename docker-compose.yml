version: '3.8'

services:
  app:
    build: .
    container_name: network-analysis-app
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
      - DATABASE_URL=sqlite:///network_analysis.db
    volumes:
      - .:/app
    command: python app.py
